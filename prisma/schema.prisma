generator client {
  provider        = "prisma-client"
  output          = "../src/generated/prisma"
  moduleFormat    = "cjs"
}


datasource db {
  provider = "postgresql"
}

model auth_group {
  id                     Int                      @id @default(autoincrement())
  name                   String                   @unique @db.VarChar(150)
  auth_group_permissions auth_group_permissions[]
  auth_user_groups       auth_user_groups[]
  filer_folderpermission filer_folderpermission[]

  @@index([name], map: "auth_group_name_a6ea08ec_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model auth_group_permissions {
  id              BigInt          @id @default(autoincrement())
  group_id        Int
  permission_id   Int
  auth_permission auth_permission @relation(fields: [permission_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_group_permissio_permission_id_84c5c92e_fk_auth_perm")
  auth_group      auth_group      @relation(fields: [group_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_group_permissions_group_id_b120cbf9_fk_auth_group_id")

  @@unique([group_id, permission_id], map: "auth_group_permissions_group_id_permission_id_0cd325b0_uniq")
  @@index([group_id], map: "auth_group_permissions_group_id_b120cbf9")
  @@index([permission_id], map: "auth_group_permissions_permission_id_84c5c92e")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model auth_permission {
  id                         Int                          @id @default(autoincrement())
  name                       String                       @db.VarChar(255)
  content_type_id            Int
  codename                   String                       @db.VarChar(100)
  auth_group_permissions     auth_group_permissions[]
  django_content_type        django_content_type          @relation(fields: [content_type_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_permission_content_type_id_2f476e4b_fk_django_co")
  auth_user_user_permissions auth_user_user_permissions[]

  @@unique([content_type_id, codename], map: "auth_permission_content_type_id_codename_01ab375a_uniq")
  @@index([content_type_id], map: "auth_permission_content_type_id_2f476e4b")
}

model auth_user {
  id                         Int                          @id @default(autoincrement())
  password                   String                       @db.VarChar(128)
  last_login                 DateTime?                    @db.Timestamptz(6)
  is_superuser               Boolean
  username                   String                       @unique @db.VarChar(150)
  first_name                 String                       @db.VarChar(150)
  last_name                  String                       @db.VarChar(150)
  email                      String                       @db.VarChar(254)
  is_staff                   Boolean
  is_active                  Boolean
  date_joined                DateTime                     @db.Timestamptz(6)
  auth_user_groups           auth_user_groups[]
  auth_user_user_permissions auth_user_user_permissions[]
  django_admin_log           django_admin_log[]
  filer_clipboard            filer_clipboard[]
  filer_file                 filer_file[]
  filer_folder               filer_folder[]
  filer_folderpermission     filer_folderpermission[]

  @@index([username], map: "auth_user_username_6821ab7c_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model auth_user_groups {
  id         BigInt     @id @default(autoincrement())
  user_id    Int
  group_id   Int
  auth_group auth_group @relation(fields: [group_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_user_groups_group_id_97559544_fk_auth_group_id")
  auth_user  auth_user  @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_user_groups_user_id_6a12ed8b_fk_auth_user_id")

  @@unique([user_id, group_id], map: "auth_user_groups_user_id_group_id_94350c0c_uniq")
  @@index([group_id], map: "auth_user_groups_group_id_97559544")
  @@index([user_id], map: "auth_user_groups_user_id_6a12ed8b")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model auth_user_user_permissions {
  id              BigInt          @id @default(autoincrement())
  user_id         Int
  permission_id   Int
  auth_permission auth_permission @relation(fields: [permission_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_user_user_permi_permission_id_1fbb5f2c_fk_auth_perm")
  auth_user       auth_user       @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_user_user_permissions_user_id_a95ead1b_fk_auth_user_id")

  @@unique([user_id, permission_id], map: "auth_user_user_permissions_user_id_permission_id_14a6b632_uniq")
  @@index([permission_id], map: "auth_user_user_permissions_permission_id_1fbb5f2c")
  @@index([user_id], map: "auth_user_user_permissions_user_id_a95ead1b")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model About {
  id          BigInt       @id @default(autoincrement())
  title       String       @db.VarChar(200)
  content     String
  image_url   String?      @db.VarChar(255)
  image_id    Int?
  filer_image filer_image? @relation(fields: [image_id], references: [file_ptr_id], onDelete: NoAction, onUpdate: NoAction, map: "core_about_image_id_7300887f_fk_filer_image_file_ptr_id")

  @@index([image_id], map: "core_about_image_id_7300887f")
  @@map("core_about")
}

model Contact {
  id    BigInt @id @default(autoincrement())
  phone String @db.VarChar(10)
  email String @db.VarChar(254)

  @@map("core_contact")
}

model Home {
  id            BigInt @id @default(autoincrement())
  site_name     String @unique @db.VarChar
  owner_name    String @unique @db.VarChar
  primary_color String @db.VarChar

  @@index([owner_name], map: "core_home_owner_name_a8d1e7d2_like")
  @@index([site_name], map: "core_home_site_name_eff7848f_like")
  @@map("core_home")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model django_admin_log {
  id                  Int                  @id @default(autoincrement())
  action_time         DateTime             @db.Timestamptz(6)
  object_id           String?
  object_repr         String               @db.VarChar(200)
  action_flag         Int                  @db.SmallInt
  change_message      String
  content_type_id     Int?
  user_id             Int
  django_content_type django_content_type? @relation(fields: [content_type_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_admin_log_content_type_id_c4bce8eb_fk_django_co")
  auth_user           auth_user            @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_admin_log_user_id_c564eba6_fk_auth_user_id")

  @@index([content_type_id], map: "django_admin_log_content_type_id_c4bce8eb")
  @@index([user_id], map: "django_admin_log_user_id_c564eba6")
}

model django_content_type {
  id               Int                @id @default(autoincrement())
  app_label        String             @db.VarChar(100)
  model            String             @db.VarChar(100)
  auth_permission  auth_permission[]
  django_admin_log django_admin_log[]
  filer_file       filer_file[]

  @@unique([app_label, model], map: "django_content_type_app_label_model_76bd3d3b_uniq")
}

model django_migrations {
  id      BigInt   @id @default(autoincrement())
  app     String   @db.VarChar(255)
  name    String   @db.VarChar(255)
  applied DateTime @db.Timestamptz(6)
}

model django_session {
  session_key  String   @id @db.VarChar(40)
  session_data String
  expire_date  DateTime @db.Timestamptz(6)

  @@index([expire_date], map: "django_session_expire_date_a5c62663")
  @@index([session_key], map: "django_session_session_key_c0390e0f_like")
}

model easy_thumbnails_source {
  id                        Int                         @id @default(autoincrement())
  storage_hash              String                      @db.VarChar(40)
  name                      String                      @db.VarChar(255)
  modified                  DateTime                    @db.Timestamptz(6)
  easy_thumbnails_thumbnail easy_thumbnails_thumbnail[]

  @@unique([storage_hash, name], map: "easy_thumbnails_source_storage_hash_name_481ce32d_uniq")
  @@index([name], map: "easy_thumbnails_source_name_5fe0edc6")
  @@index([name], map: "easy_thumbnails_source_name_5fe0edc6_like")
  @@index([storage_hash], map: "easy_thumbnails_source_storage_hash_946cbcc9")
  @@index([storage_hash], map: "easy_thumbnails_source_storage_hash_946cbcc9_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model easy_thumbnails_thumbnail {
  id                                  Int                                  @id @default(autoincrement())
  storage_hash                        String                               @db.VarChar(40)
  name                                String                               @db.VarChar(255)
  modified                            DateTime                             @db.Timestamptz(6)
  source_id                           Int
  easy_thumbnails_source              easy_thumbnails_source               @relation(fields: [source_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "easy_thumbnails_thum_source_id_5b57bc77_fk_easy_thum")
  easy_thumbnails_thumbnaildimensions easy_thumbnails_thumbnaildimensions?

  @@unique([storage_hash, name, source_id], map: "easy_thumbnails_thumbnai_storage_hash_name_source_fb375270_uniq")
  @@index([name], map: "easy_thumbnails_thumbnail_name_b5882c31")
  @@index([name], map: "easy_thumbnails_thumbnail_name_b5882c31_like")
  @@index([source_id], map: "easy_thumbnails_thumbnail_source_id_5b57bc77")
  @@index([storage_hash], map: "easy_thumbnails_thumbnail_storage_hash_f1435f49")
  @@index([storage_hash], map: "easy_thumbnails_thumbnail_storage_hash_f1435f49_like")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model easy_thumbnails_thumbnaildimensions {
  id                        Int                       @id @default(autoincrement())
  thumbnail_id              Int                       @unique
  width                     Int?
  height                    Int?
  easy_thumbnails_thumbnail easy_thumbnails_thumbnail @relation(fields: [thumbnail_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "easy_thumbnails_thum_thumbnail_id_c3a0c549_fk_easy_thum")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model filer_clipboard {
  id                  Int                   @id @default(autoincrement())
  user_id             Int
  auth_user           auth_user             @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "filer_clipboard_user_id_b52ff0bc_fk_auth_user_id")
  filer_clipboarditem filer_clipboarditem[]

  @@index([user_id], map: "filer_clipboard_user_id_b52ff0bc")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model filer_clipboarditem {
  id              Int             @id @default(autoincrement())
  clipboard_id    Int
  file_id         Int
  filer_clipboard filer_clipboard @relation(fields: [clipboard_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "filer_clipboarditem_clipboard_id_7a76518b_fk_filer_clipboard_id")
  filer_file      filer_file      @relation(fields: [file_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "filer_clipboarditem_file_id_06196f80_fk_filer_file_id")

  @@index([clipboard_id], map: "filer_clipboarditem_clipboard_id_7a76518b")
  @@index([file_id], map: "filer_clipboarditem_file_id_06196f80")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model filer_file {
  id                     Int                   @id @default(autoincrement())
  file                   String?               @db.VarChar(255)
  file_size              BigInt?               @map("_file_size")
  sha1                   String                @db.VarChar(40)
  has_all_mandatory_data Boolean
  original_filename      String?               @db.VarChar(255)
  name                   String                @db.VarChar(255)
  description            String?
  uploaded_at            DateTime              @db.Timestamptz(6)
  modified_at            DateTime              @db.Timestamptz(6)
  is_public              Boolean
  folder_id              Int?
  owner_id               Int?
  polymorphic_ctype_id   Int?
  mime_type              String                @db.VarChar(255)
  filer_clipboarditem    filer_clipboarditem[]
  filer_folder           filer_folder?         @relation(fields: [folder_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "filer_file_folder_id_af803bbb_fk_filer_folder_id")
  auth_user              auth_user?            @relation(fields: [owner_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "filer_file_owner_id_b9e32671_fk_auth_user_id")
  django_content_type    django_content_type?  @relation(fields: [polymorphic_ctype_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "filer_file_polymorphic_ctype_id_f44903c1_fk_django_co")
  filer_image            filer_image?

  @@index([folder_id], map: "filer_file_folder_id_af803bbb")
  @@index([owner_id], map: "filer_file_owner_id_b9e32671")
  @@index([polymorphic_ctype_id], map: "filer_file_polymorphic_ctype_id_f44903c1")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model filer_folder {
  id                     Int                      @id @default(autoincrement())
  name                   String                   @db.VarChar(255)
  uploaded_at            DateTime                 @db.Timestamptz(6)
  created_at             DateTime                 @db.Timestamptz(6)
  modified_at            DateTime                 @db.Timestamptz(6)
  owner_id               Int?
  parent_id              Int?
  filer_file             filer_file[]
  auth_user              auth_user?               @relation(fields: [owner_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "filer_folder_owner_id_be530fb4_fk_auth_user_id")
  filer_folder           filer_folder?            @relation("filer_folderTofiler_folder", fields: [parent_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "filer_folder_parent_id_308aecda_fk_filer_folder_id")
  other_filer_folder     filer_folder[]           @relation("filer_folderTofiler_folder")
  filer_folderpermission filer_folderpermission[]

  @@unique([parent_id, name], map: "filer_folder_parent_id_name_bc773258_uniq")
  @@index([owner_id], map: "filer_folder_owner_id_be530fb4")
  @@index([parent_id], map: "filer_folder_parent_id_308aecda")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model filer_folderpermission {
  id               Int           @id @default(autoincrement())
  type             Int           @db.SmallInt
  everybody        Boolean
  can_edit         Int?          @db.SmallInt
  can_read         Int?          @db.SmallInt
  can_add_children Int?          @db.SmallInt
  folder_id        Int?
  group_id         Int?
  user_id          Int?
  filer_folder     filer_folder? @relation(fields: [folder_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "filer_folderpermission_folder_id_5d02f1da_fk_filer_folder_id")
  auth_group       auth_group?   @relation(fields: [group_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "filer_folderpermission_group_id_8901bafa_fk_auth_group_id")
  auth_user        auth_user?    @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "filer_folderpermission_user_id_7673d4b6_fk_auth_user_id")

  @@index([folder_id], map: "filer_folderpermission_folder_id_5d02f1da")
  @@index([group_id], map: "filer_folderpermission_group_id_8901bafa")
  @@index([user_id], map: "filer_folderpermission_user_id_7673d4b6")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model filer_image {
  file_ptr_id                       Int             @id
  height                            Float?          @map("_height")
  width                             Float?          @map("_width")
  date_taken                        DateTime?       @db.Timestamptz(6)
  default_alt_text                  String?         @db.VarChar(255)
  default_caption                   String?         @db.VarChar(255)
  author                            String?         @db.VarChar(255)
  must_always_publish_author_credit Boolean
  must_always_publish_copyright     Boolean
  subject_location                  String          @db.VarChar(64)
  transparent                       Boolean         @map("_transparent")
  core_about                        About[]
  core_hardware                     core_hardware[]
  core_software                     core_software[]
  filer_file                        filer_file      @relation(fields: [file_ptr_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "filer_image_file_ptr_id_3e21d4f0_fk_filer_file_id")
}

model filer_thumbnailoption {
  id      Int     @id @default(autoincrement())
  name    String  @db.VarChar(100)
  width   Int
  height  Int
  crop    Boolean
  upscale Boolean
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model SocialNetwork {
  id        BigInt  @id @default(autoincrement())
  name      String  @db.VarChar(50)
  link      String  @db.VarChar(200)
  icon      String  @db.VarChar
  is_active Boolean
  order     Int

  @@map("core_socialnetwork")
}

model User {
  id        BigInt   @id @default(autoincrement())
  username  String   @unique
  email     String   @unique
  password  String
  is_active Boolean  @default(true)
  Review    Review[]

  @@index([email], map: "User_email_667201b5_like")
  @@index([username], map: "User_username_3851fdce_like")
  @@map("User")
}

model Service {
  id          BigInt  @id @default(autoincrement())
  name        String  @unique @db.VarChar(200)
  description String
  price       Int?
  icon        String  @db.VarChar(50)
  order       Int
  is_active   Boolean

  @@index([name], map: "core_service_name_8e9e4033_like")
  @@map("core_service")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model Review {
  id          BigInt   @id @default(autoincrement())
  content     String
  rating      Int
  createdAt   DateTime @default(dbgenerated("statement_timestamp()")) @db.Timestamptz(6)
  is_active   Boolean  @default(true)
  userId      BigInt
  core_status String   @default("pending") @db.VarChar(20)
  User        User     @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "Review_userId_c5024530_fk_User_id")

  @@index([userId], map: "Review_userId_c5024530")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model core_hardware {
  id          BigInt       @id @default(autoincrement())
  name        String       @db.VarChar(200)
  price       Int
  description String
  is_active   Boolean
  order       Int
  icon        String       @db.VarChar(50)
  warranty    String       @db.VarChar(200)
  image_id    Int?
  image_url   String?      @db.VarChar(255)
  filer_image filer_image? @relation(fields: [image_id], references: [file_ptr_id], onDelete: NoAction, onUpdate: NoAction, map: "core_hardware_image_id_3f2b0e08_fk_filer_image_file_ptr_id")

  @@index([image_id], map: "core_hardware_image_id_3f2b0e08")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model core_software {
  id          BigInt       @id @default(autoincrement())
  name        String       @db.VarChar(200)
  price       Int
  description String
  is_active   Boolean
  order       Int
  icon        String       @db.VarChar(50)
  version     String       @db.VarChar(200)
  image_id    Int?
  image_url   String?      @db.VarChar(255)
  filer_image filer_image? @relation(fields: [image_id], references: [file_ptr_id], onDelete: NoAction, onUpdate: NoAction, map: "core_software_image_id_c7d819b6_fk_filer_image_file_ptr_id")

  @@index([image_id], map: "core_software_image_id_c7d819b6")
}
